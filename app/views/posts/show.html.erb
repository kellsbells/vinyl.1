<br><br><br><br><br>
<div class="container">
      <div class="row">
      <div class="col-md-5  toppad  pull-right col-md-offset-3 ">
<p class=" text-info">Posted on <%= @post.created_at.strftime(" %b %d %I:%M %p") %></p>
      </div>
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xs-offset-0 col-sm-offset-0 col-md-offset-3 col-lg-offset-3 toppad" >
   
          <div class="panel panel-info">
            <div class="panel-heading">
              <h4>Posted by <%= link_to @post.user.username, user_path(@post.user.id) %></h4>
            </div>
            <div class="panel-body">
              <div class="row">
                <div class="col-md-3 col-lg-3 " align="center"><%= image_tag @post.image_url(:small_thumb).to_s %></div>
                <div class=" col-md-9 col-lg-9 "> 
                  <table class="table table-user-information">
                    <tbody>
                      <tr>
                        <td>Artist:</td>
                        <td><%= @post.artist %></td>
                      </tr>
                      <tr>
                        <td>Album:</td>
                        <td><%= @post.album_name %></td>
                      </tr>
                      <tr>
                        <td>Year of Release</td>
                        <td><%= @post.year %></td>
                      </tr>
                   
                         <tr>
                             <tr>
                        <td>Description</td>
                        <td><%= simple_format (@post.description) %></td>
                      </tr>
                     
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
                 <div class="panel-footer">
                        <i><%= link_to 'Home',
                                  posts_path, :class => 'btn btn-sky text-uppercase'  %></i>
                        <span class="pull-right">
                          <div class="form-group">
                            <%# if current_user.present? %>
                            <% if current_user.present? && current_user.id == @post.user.id %>
                            <%= link_to t('.edit', :default => t("helpers.links.edit")),
                                        edit_post_path(@post), :class => ' btn btn-sky text-uppercase' %>
                            <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                                    post_path(@post),
                                    :method => 'delete',
                                    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                                    :class => 'btn btn-sky text-uppercase' %>
                            <% end %>
                            <%# end %>
                          </div>
                        </span>
                    </div>
            
          </div>
        </div>
      </div>
    </div>