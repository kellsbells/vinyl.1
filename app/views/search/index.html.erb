<h1>Search Results</h1>

<ul class='results'>
  <% if @results.blank? %>
    <li><i>No Results</i></li>
  <% else %>
    <% @results.each do |result| %>
      <li>
        <%= link_to result.try(:artist), post_path(result.id) %>
      </li>
    <% end %>
  <% end %>
</ul>